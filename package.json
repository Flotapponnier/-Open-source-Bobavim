{
  "name": "boba-vim",
  "version": "1.0.0",
  "type": "module",
  "description": "Vim learning game with performance optimizations",
  "scripts": {
    "build": "vite build",
    "build:dev": "vite build --mode development",
    "watch": "vite build --watch",
    "dev": "vite"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
