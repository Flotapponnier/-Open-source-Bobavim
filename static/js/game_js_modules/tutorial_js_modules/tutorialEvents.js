import { toggleTutorialMode } from './tutorialMode.js';

export function initializeTutorialMode() {
  // Tutorial toggle now handled by Space+T combination in movement handler
  // Old key binding (+) disabled
}